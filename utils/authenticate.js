
const { Response } = require("./response");

var jwt = require('jsonwebtoken');
const database = require("./database");
// const { SkeinAuthentication, Authorize } = require("../user-management/dist/utils/authenticate");




const Authenticate = {
    // Access: Authorize,
    // Authenticate: SkeinAuthentication
}

module.exports = Authenticate